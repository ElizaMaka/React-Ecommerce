import React, { useState } from 'react'

const ToogleOrderNowBar = () => {
    const  [show, setShow] = useState(false);

    function toggle(){
        setShow
    }
}

export default ToogleOrderNowBar
